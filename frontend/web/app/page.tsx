import React from "react";
import { Metadata } from "next";

const page: React.FC = () => {
  return <div>Home</div>;
};

export default page;

export const metadata: Metadata = {
  title: `Turbo App | Home`,
};
